{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the main + popover windows. The popover needs the same event/IPC surface as main so it can listen to `core-event`, invoke session start/stop, and dismiss itself via `window.hide()`. Without `popover` in this list, the popover webview gets zero capabilities — Start/Stop buttons fire IPC but never see the resulting state change, and 'Open window' can't close the popover.",
  "windows": ["main", "popover"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-hide",
    "core:window:allow-show",
    "core:window:allow-set-focus",
    "core:window:allow-unminimize",
    "core:webview:allow-internal-toggle-devtools",
    "core:tray:default",
    "core:menu:default",
    "core:event:default",
    "core:app:default",
    "core:path:default",
    "core:image:default",
    "dialog:default",
    "notification:default",
    "shell:default",
    "opener:default",
    "updater:default",
    "process:default"
  ]
}
